name: auth_repository
description:  A Dart package for managing the athentication domain.

environment:
  sdk:  ">=2.16.0 <3.0.0"

dependencies:
  firebase_auth: ^3.4.1
  firebase_core: ^1.19.1
  formz: ^0.4.1
  google_sign_in: ^5.4.0
  meta:
  cache:
    path: ../cache
  equatable: ^2.0.3
  
dev_dependencies:
  very_good_analysis: ^3.0.1